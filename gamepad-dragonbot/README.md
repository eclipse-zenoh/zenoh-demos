> **DEPRECATED** — zenoh-flow v0.3.0 is unmaintained and has no active successor.
> This demo will not compile against zenoh ≥ 1.x and will not be updated.
> It is kept for historical reference only.

# gamepad-dragonbot

Zenoh Flow teleop pipeline (source-gamepad → operator-twist → sink-serialize) that drove the ZettaScale DragonBot demo robot.

Depended on `eclipse-zenoh/zenoh-flow` tag `v0.3.0` which is no longer maintained.
